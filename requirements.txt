# Core dependencies — all typically pre-installed on macOS dev systems
opencv-python>=4.8.0     # DIS optical flow (primary fallback interpolation)
Pillow>=10.0.0           # Frame I/O
numpy>=1.24.0            # Array ops

# SOTA interpolation — already present on Apple Silicon Macs
# torch>=2.0.0           # Required by RIFE; install via pytorch.org if not present

# RIFE model code is auto-downloaded to ~/.video-repair/rife/ via:
#   python repair.py --setup-rife
